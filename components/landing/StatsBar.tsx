'use client';

interface StatsBarProps {
  version: string;
  activeKeys: number;
  totalSlots: number;
  maintenanceOn: boolean;
}

export function StatsBar(_props: StatsBarProps) {
  return null;
}
